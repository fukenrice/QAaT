1. while (Math.abs(x - res * res) / x > eps) {
2. arg = 1.0600000000000005. На этих данных кол-во итераций будет больше 5, но результат совпадет с Math.sqrt
3. Таких чисел не нашел, но кол-во итераций в изначальной реализации неограничено, значит может уйти в бесконечный цикл
4. while (Math.abs(x - res * res) / x > eps && i < 5) {